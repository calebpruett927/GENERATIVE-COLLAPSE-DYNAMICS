schema: "schemas/closures_registry.schema.json"

registry:
  id: "kinematics_complete.closures"

  closures:
    gamma:
      path: "closures/gamma.default.v1.yaml"
    return_domain:
      path: "closures/return_domain.window64.v1.yaml"
    norms:
      path: "closures/norms.l2_eta1e-3.v1.yaml"
    curvature_neighborhood:
      path: "closures/curvature_neighborhood.default.v1.yaml"

  notes: "Kinematics domain closures referencing base UMCP closure definitions. Domain-specific computation in closures/kinematics/ (linear, rotational, energy, momentum, phase space return, kinematic stability)."
