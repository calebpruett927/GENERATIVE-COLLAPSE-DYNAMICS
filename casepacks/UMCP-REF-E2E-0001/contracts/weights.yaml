schema: "schemas/weights.schema.json"

weights:
  id: "UMCP-REF-E2E-0001.weights"
  version: "1.0.0"
  policy: "nonnegative_sum_to_one"

  channels:
    - name: "c_1"
      label: "Voltage channel"
      weight: 0.333333333333333
    - name: "c_2"
      label: "Temperature channel"
      weight: 0.333333333333333
    - name: "c_3"
      label: "Pressure channel"
      weight: 0.333333333333334

  validation:
    sum: 1.0
    tolerance: 1.0e-9

  notes: "Uniform weights across 3 channels. Σw_i = 1.0 exactly."
