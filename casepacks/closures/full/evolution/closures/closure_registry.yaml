schema: "schemas/closures_registry.schema.json"

registry:
  id: "evolution_kernel.closures"

  closures:
    gamma:
      path: "closures/gamma.default.v1.yaml"
    return_domain:
      path: "closures/return_domain.window64.v1.yaml"
    norms:
      path: "closures/norms.l2_eta1e-3.v1.yaml"
    curvature_neighborhood:
      path: "closures/curvature_neighborhood.default.v1.yaml"

  notes: "Evolution domain closures referencing base UMCP closure definitions. Domain-specific computation in closures/evolution/evolution_kernel.py (40 organisms × 8 channels) and closures/evolution/recursive_evolution.py (5 scales + 5 mass extinctions)."
