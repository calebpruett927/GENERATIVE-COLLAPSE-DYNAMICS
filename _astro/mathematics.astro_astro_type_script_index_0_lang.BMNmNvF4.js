import{c as b,a as f,v as y,e as v}from"./kernel.1iDFzIM1.js";import{a as l,b as x}from"./constants.DtcEBpaC.js";const C=[{symbol:"F",name:"Fidelity",latin:"Fidelitas",formula:"F = Σ wᵢcᵢ",status:"Primitive",statusColor:"text-amber-400",color:"text-amber-400",borderColor:"border-amber-700 hover:border-amber-500",detail:`<p><strong>What it measures:</strong> The weighted arithmetic mean of all channels — how much of the original signal survives collapse.</p>
        <p><strong>Range:</strong> [0, 1]. For equal weights, F = mean(c).</p>
        <p><strong>Role in the algebra:</strong> Determines ω (drift = 1 − F). Provides the upper bound for IC. Composes arithmetically: F₁₂ = (F₁ + F₂)/2.</p>
        <p><strong>Sensitivity:</strong> ∂F/∂cᵢ = wᵢ. Lipschitz: |ΔF| ≤ δ for perturbation δ.</p>
        <p class="text-kernel-500 text-xs mt-2 italic">Quid supersit post collapsum — what survives collapse.</p>`},{symbol:"κ",name:"Log-Integrity",latin:"Log-Integritas",formula:"κ = Σ wᵢ ln(cᵢ,ε)",status:"Primitive",statusColor:"text-amber-400",color:"text-purple-400",borderColor:"border-purple-700 hover:border-purple-500",detail:`<p><strong>What it measures:</strong> The weighted sum of logarithms of ε-clamped channels — logarithmic sensitivity of coherence.</p>
        <p><strong>Range:</strong> κ ≤ 0 (since ln(cᵢ) ≤ 0 for cᵢ ∈ (0, 1]).</p>
        <p><strong>Why logarithmic?</strong> The log transforms the multiplicative structure of IC into an additive one. A single dead channel (cᵢ → 0) sends κ → −∞.</p>
        <p><strong>Guard band:</strong> cᵢ,ε = max(cᵢ, ε) prevents the pole at c = 0. With ε = 10⁻⁸, the guard doesn't affect any measurement to machine precision.</p>
        <p><strong>Sensitivity:</strong> |∂κ/∂cᵢ| ≤ wᵢ/ε — most sensitive near c = 0.</p>`},{symbol:"S",name:"Entropy",latin:"Entropia",formula:"S = −Σ wᵢ[cᵢ ln cᵢ + (1−cᵢ)ln(1−cᵢ)]",status:"Primitive*",statusColor:"text-amber-400",color:"text-teal-400",borderColor:"border-teal-700 hover:border-teal-500",detail:`<p><strong>What it measures:</strong> The Bernoulli field entropy — uncertainty of the collapse field. NOT Shannon entropy.</p>
        <p><strong>Range:</strong> [0, ln 2] per channel. Maximum at c = 1/2 ("equator").</p>
        <p><strong>Why "Primitive*"?</strong> Computed directly from the trace, but asymptotically not free — determined by F and C as n → ∞ (CLT). So it is a computed output that is not an independent degree of freedom.</p>
        <p><strong>Classical limit:</strong> When cᵢ ∈ {0, 1} only, the Bernoulli field collapses and S reduces to Shannon entropy.</p>
        <p class="text-kernel-500 text-xs mt-2 italic">Shannon est limes degener — Shannon is the degenerate limit.</p>`},{symbol:"C",name:"Curvature",latin:"Curvatura",formula:"C = std_pop(cᵢ) / 0.5",status:"Primitive",statusColor:"text-amber-400",color:"text-orange-400",borderColor:"border-orange-700 hover:border-orange-500",detail:`<p><strong>What it measures:</strong> Channel heterogeneity — coupling to uncontrolled degrees of freedom. Normalized population standard deviation.</p>
        <p><strong>Range:</strong> [0, 1]. C = 0 when all channels are equal (homogeneous). C = 1 is maximum heterogeneity.</p>
        <p><strong>Why "Curvature"?</strong> Named for its role: it measures the bending / divergence of channels from uniformity. In Fisher space, it drives the cost D_C = αC.</p>
        <p><strong>Independence:</strong> C is the only genuinely independent primitive besides F and κ. It cannot be determined from the other invariants — this is why rank-3 systems have 3 DOF.</p>`},{symbol:"ω",name:"Drift",latin:"Derivatio",formula:"ω = 1 − F",status:"Derived",statusColor:"text-blue-400",color:"text-red-400",borderColor:"border-red-700 hover:border-red-500",detail:`<p><strong>What it measures:</strong> Collapse proximity — how much is lost. The complement of fidelity.</p>
        <p><strong>Identity:</strong> F + ω = 1 (exact to 0.0e+00 across 10K traces). This is the duality identity — the first structural identity of the system.</p>
        <p><strong>Regime gates:</strong> ω < 0.038 → Stable, 0.038 ≤ ω < 0.30 → Watch, ω ≥ 0.30 → Collapse.</p>
        <p><strong>In the budget:</strong> Powers the drift cost Γ(ω) = ω³/(1−ω+ε). Cubic because p = 3 is the unique integer yielding the Cardano root.</p>
        <p class="text-kernel-500 text-xs mt-2 italic">Quantum collapsu deperdatur — how much is lost to collapse.</p>`},{symbol:"IC",name:"Integrity",latin:"Integritas Composita",formula:"IC = exp(κ)",status:"Derived",statusColor:"text-blue-400",color:"text-green-400",borderColor:"border-green-700 hover:border-green-500",detail:`<p><strong>What it measures:</strong> Multiplicative coherence — the weighted geometric mean of channels. One dead channel kills IC regardless of how healthy the rest are.</p>
        <p><strong>Identity:</strong> IC ≤ F (the integrity bound). This is not AM-GM — it is the solvability condition: for n=2, c₁,₂ = F ± √(F² − IC²) requires IC ≤ F for real solutions.</p>
        <p><strong>Composition:</strong> IC composes geometrically: IC₁₂ = √(IC₁ · IC₂). This contrasts with F which composes arithmetically.</p>
        <p><strong>The heterogeneity gap:</strong> Δ = F − IC ≈ Var(c)/(2c̄). This is the Fisher Information contribution from heterogeneity.</p>
        <p class="text-kernel-500 text-xs mt-2 italic">Limbus integritatis: IC ≤ F — the hem-edge of integrity.</p>`}];function k(){const o=document.getElementById("invariant-cards");o.innerHTML="",C.forEach((e,r)=>{const t=document.createElement("button");t.className=`inv-card bg-kernel-950 border-2 ${e.borderColor} rounded-xl p-3 text-center cursor-pointer transition group`,t.dataset.index=String(r),t.innerHTML=`
        <div class="text-2xl font-bold font-mono ${e.color} mb-0.5">${e.symbol}</div>
        <div class="text-xs font-bold text-kernel-200">${e.name}</div>
        <div class="text-[10px] text-kernel-500 italic">${e.latin}</div>
        <div class="text-[10px] font-mono text-kernel-600 mt-1">${e.formula}</div>
        <div class="text-[10px] ${e.statusColor} mt-1">${e.status}</div>
      `,t.addEventListener("click",()=>n("invariant-detail","inv-detail-title","inv-detail-body",`<span class="${e.color}">${e.symbol}</span> — ${e.name} <span class="text-kernel-500 text-sm">(${e.latin})</span>`,`<div class="bg-kernel-900 rounded p-3 mb-3"><span class="font-mono ${e.color}">${e.formula}</span></div>${e.detail}`,".inv-card",r)),o.appendChild(t)})}const w=[{formula:"F + ω = 1",name:"Duality Identity",color:"text-amber-400",borderColor:"border-amber-700 hover:border-amber-500",tag:"E1",detail:`<p>In Fisher coordinates: sin²θ + cos²θ = 1. The identity is <em>exact</em> — residual is 0.0e+00 across 10K random traces.</p>
        <p><strong>Why it holds:</strong> ω is <em>defined</em> as 1 − F. This is not empirical — it is a definition that captures a structural duality: what survives (F) and what is lost (ω) partition unity.</p>
        <p><strong>What it implies:</strong> No third category. Every measurement partitions into fidelity and drift. <em>Tertia via nulla — no third possibility.</em></p>
        <p><strong>Classical limit:</strong> Strip the cost function and the phase diagram → probability conservation / probability normalization.</p>`},{formula:"IC ≤ F",name:"Integrity Bound",color:"text-green-400",borderColor:"border-green-700 hover:border-green-500",tag:"E3",detail:`<p>The integrity bound is <em>not</em> the AM-GM inequality, though AM-GM emerges as a degenerate limit when you strip the channel semantics, weights, guard band, and composition laws.</p>
        <p><strong>Proof route (Solvability):</strong> For n = 2 channels with known F and IC, recover c₁,₂ = F ± √(F² − IC²). The discriminant must be non-negative, hence IC ≤ F.</p>
        <p><strong>Proof route (Perturbation Chain):</strong> A6 → B3 → A2. From Taylor expansion: κ ≈ ln F − C²/(8F²) + O(C⁴). Since the correction −C²/(8F²) is always negative, exp(κ) ≤ F.</p>
        <p><strong>What's extra vs AM-GM:</strong> channel semantics (what channels mean), weights (non-uniform), guard band (ε-clamp), solvability (trace recovery), and composition laws (IC geometric, F arithmetic).</p>
        <p><strong>Equality:</strong> IC = F iff all cᵢ = c₀ (Rank-1, homogeneous). The gap Δ = F − IC measures heterogeneity.</p>`},{formula:"IC = exp(κ)",name:"Log-Integrity Relation",color:"text-purple-400",borderColor:"border-purple-700 hover:border-purple-500",tag:"E2",detail:`<p>Links the multiplicative world (IC, geometric mean) to the additive world (κ, sum of logs). This is the structural bridge.</p>
        <p><strong>Why it holds:</strong> IC = ∏cᵢ^{wᵢ}. Taking log: ln(IC) = Σ wᵢ ln(cᵢ) = κ. Exponentiate: IC = exp(κ).</p>
        <p><strong>Consequence:</strong> Additive operations on κ become multiplicative operations on IC. Seam composition is additive in κ-space.</p>
        <p><strong>Classical limit:</strong> Strip domain universality and global scope → the exponential map (local, chart-specific).</p>`}];function F(){const o=document.getElementById("identity-cards-algebra");o.innerHTML="",w.forEach((e,r)=>{const t=document.createElement("button");t.className=`alg-id-card flex items-center gap-4 w-full bg-kernel-950 border-2 ${e.borderColor} rounded-lg p-4 cursor-pointer transition text-left group`,t.dataset.index=String(r),t.innerHTML=`
        <span class="font-mono text-xs text-kernel-600 shrink-0">${e.tag}</span>
        <span class="font-mono text-lg ${e.color} font-bold shrink-0">${e.formula}</span>
        <span class="flex-1 text-sm text-kernel-300">${e.name}</span>
        <span class="text-[10px] text-kernel-600 group-hover:text-kernel-400 transition shrink-0">Click →</span>
      `,t.addEventListener("click",()=>n("algebra-identity-detail","alg-id-title","alg-id-body",`<span class="${e.color}">${e.formula}</span> — ${e.name}`,e.detail,".alg-id-card",r)),o.appendChild(t)})}const T=[{formula:"F₁₂ = (F₁ + F₂) / 2",name:"Fidelity composes arithmetically",color:"text-amber-400",borderColor:"border-amber-700 hover:border-amber-500",detail:`<p>When two subsystems are combined, their fidelities average. This is the <em>arithmetic</em> composition.</p>
        <p>A system with F₁ = 0.9 and F₂ = 0.5 composes to F₁₂ = 0.7. No surprises — the mean is linear.</p>
        <p><strong>Contrast with IC:</strong> IC composes geometrically, so IC₁₂ is dragged down harder by the weaker system.</p>`},{formula:"IC₁₂ = √(IC₁ · IC₂)",name:"Integrity composes geometrically",color:"text-green-400",borderColor:"border-green-700 hover:border-green-500",detail:`<p>Integrity uses the geometric mean — the weakest link drags down the composite more than the arithmetic mean would.</p>
        <p>IC₁ = 0.9, IC₂ = 0.1 → IC₁₂ = √(0.09) = 0.3. Compare: F₁₂ = 0.5 looks healthier than the integrity reveals.</p>
        <p><strong>This is the key insight:</strong> F lies, IC tells the truth. Composition amplifies the divergence (the heterogeneity gap grows).</p>`},{formula:"Δ₁₂ = (Δ₁+Δ₂)/2 + (√IC₁−√IC₂)²/2",name:"Gap composition with Hellinger correction",color:"text-teal-400",borderColor:"border-teal-700 hover:border-teal-500",detail:`<p>The heterogeneity gap has a <em>non-trivial</em> composition law. The first term is the arithmetic mean of the gaps. The second is a Hellinger-like correction that measures how different the two subsystems are.</p>
        <p><strong>If subsystems are identical:</strong> Δ₁₂ = Δ₁ (gap is a composition invariant for identical systems).</p>
        <p><strong>If subsystems differ:</strong> The Hellinger correction is always ≥ 0, so Δ₁₂ ≥ (Δ₁+Δ₂)/2. Composing unequal systems increases the gap.</p>`},{formula:"s₁₂ = s₁ + s₂",name:"Seam residuals compose additively",color:"text-cyan-400",borderColor:"border-cyan-700 hover:border-cyan-500",detail:`<p>Seam residuals add. This makes multi-step validation O(1) — you only need the running total, not the full history.</p>
        <p>The seam algebra is associative and abelian (commutative). Combined with the zero seam (identity), it forms an abelian monoid.</p>
        <p><strong>Verified:</strong> Associativity error = 5.55 × 10⁻¹⁷ (machine precision).</p>`}];function I(){const o=document.getElementById("composition-cards");o.innerHTML="",T.forEach((e,r)=>{const t=document.createElement("button");t.className=`comp-card flex items-center gap-4 w-full bg-kernel-950 border-2 ${e.borderColor} rounded-lg p-4 cursor-pointer transition text-left group`,t.dataset.index=String(r),t.innerHTML=`
        <span class="font-mono text-sm ${e.color} font-bold shrink-0 w-56">${e.formula}</span>
        <span class="flex-1 text-sm text-kernel-300">${e.name}</span>
        <span class="text-[10px] text-kernel-600 group-hover:text-kernel-400 transition shrink-0">Click →</span>
      `,t.addEventListener("click",()=>n("composition-detail","comp-title","comp-body",`<span class="${e.color}">${e.formula}</span>`,e.detail,".comp-card",r)),o.appendChild(t)})}const $=[{output:"F",derivative:"∂F/∂cᵢ = wᵢ",lipschitz:"|ΔF| ≤ δ",color:"text-amber-400",borderColor:"border-amber-700 hover:border-amber-500",detail:`<p>Fidelity is a linear function of the channels — its derivative is simply the weight. A perturbation δ in any channel changes F by at most δ.</p>
        <p>This means F is the <em>least sensitive</em> kernel output. It smooths over channel-level structure. This is both its strength (stability) and its weakness (it hides problems).</p>`},{output:"κ",derivative:"∂κ/∂cᵢ = wᵢ/cᵢ,ε",lipschitz:"|Δκ| ≤ δ/ε",color:"text-purple-400",borderColor:"border-purple-700 hover:border-purple-500",detail:`<p>Log-integrity has a 1/c singularity — sensitivity diverges as c → 0. The ε-clamp bounds this at wᵢ/ε = wᵢ × 10⁸.</p>
        <p><strong>Physical meaning:</strong> κ is maximally sensitive to channels near zero. This is <em>by design</em> — it's why IC detects dead channels that F misses.</p>
        <p><strong>Lipschitz:</strong> |Δκ| ≤ δ/ε. For ε = 10⁻⁸, a δ = 10⁻¹⁰ perturbation changes κ by at most 0.01.</p>`},{output:"S",derivative:"∂S/∂cᵢ = wᵢ · ln[(1−cᵢ)/cᵢ]",lipschitz:"|ΔS| ≤ ln((1−ε)/ε) · δ",color:"text-teal-400",borderColor:"border-teal-700 hover:border-teal-500",detail:`<p>Entropy sensitivity vanishes at c = 1/2 (the equator) — where S is maximal, it is locally flat. Maximum sensitivity is at the poles (c → 0 or c → 1).</p>
        <p><strong>Second derivative:</strong> ∂²S/∂cᵢ² = −wᵢ/[cᵢ(1−cᵢ)] = −wᵢ · g_F(cᵢ). The Hessian IS the Fisher metric (up to sign). This is the Fano-Fisher duality (N5): h″(c) = −g_F(c).</p>`},{output:"C",derivative:"complex (pop std)",lipschitz:"|ΔC| ≤ 2δ",color:"text-orange-400",borderColor:"border-orange-700 hover:border-orange-500",detail:`<p>Curvature depends on population standard deviation — its derivative involves the deviation of each channel from the mean. A single perturbation δ changes C by at most 2δ.</p>
        <p>C is the only purely independent primitive — it cannot be recovered from F, κ, or S alone. This independence is what creates rank-3 systems.</p>`}];function S(){const o=document.getElementById("sensitivity-cards");o.innerHTML="",$.forEach((e,r)=>{const t=document.createElement("button");t.className=`sens-card flex items-center gap-4 w-full bg-kernel-950 border-2 ${e.borderColor} rounded-lg p-4 cursor-pointer transition text-left group`,t.dataset.index=String(r),t.innerHTML=`
        <span class="font-mono text-lg ${e.color} font-bold shrink-0 w-8">${e.output}</span>
        <span class="font-mono text-xs text-kernel-400 w-40 shrink-0">${e.derivative}</span>
        <span class="font-mono text-xs text-kernel-500 flex-1">${e.lipschitz}</span>
        <span class="text-[10px] text-kernel-600 group-hover:text-kernel-400 transition shrink-0">Click →</span>
      `,t.addEventListener("click",()=>n("sensitivity-detail","sens-title","sens-body",`<span class="${e.color}">∂${e.output}/∂c</span> — Sensitivity of ${e.output}`,e.detail,".sens-card",r)),o.appendChild(t)})}const E=[{name:"Log-Integrity Perturbation",formula:"κ ≈ ln F − C²/(8F²) + O(C⁴)",correction:"−C²/(8F²)",color:"text-purple-400",borderColor:"border-purple-700 hover:border-purple-500",detail:`<p>Near homogeneity (small C), κ deviates from ln F by a correction proportional to C². The correction is <em>always negative</em> — heterogeneity always reduces log-integrity.</p>
        <p><strong>This is the perturbation chain:</strong> A6 → B3 → A2. The Taylor structure <em>proves</em> IC ≤ F, because exp(κ) = exp(ln F − C²/(8F²) + ...) = F · exp(−C²/(8F²)) ≤ F.</p>
        <p><strong>Magnitude:</strong> For F = 0.8, C = 0.2: correction = −0.0078. Small but always one-signed.</p>`},{name:"Heterogeneity Gap",formula:"Δ ≈ C²/(8F) for small C",correction:"C²/(8F)",color:"text-teal-400",borderColor:"border-teal-700 hover:border-teal-500",detail:`<p>The gap Δ = F − IC grows quadratically with curvature. For small heterogeneity, the Fisher Information formula holds: Δ ≈ Var(c)/(2c̄) = C²/(8F).</p>
        <p><strong>Breakdown:</strong> Below c̄ ≈ 0.25, the approximation error exceeds 10% — this is the Poisson limit where noise is proportional to signal. The exact gap must be used.</p>`},{name:"Entropy Near Equator",formula:"S ≈ ln 2 − 2(c − 1/2)² + O((c − 1/2)⁴)",correction:"−2(c−1/2)²",color:"text-green-400",borderColor:"border-green-700 hover:border-green-500",detail:`<p>Near the equator c = 1/2, entropy is locally quadratic. The correction is negative — S has a maximum at c = 1/2 (well known: Bernoulli entropy peaks at p = 1/2).</p>
        <p><strong>Curvature at peak:</strong> h″(1/2) = −4 = −g_F(1/2). This is the Fano-Fisher duality in action.</p>`}];function L(){const o=document.getElementById("taylor-cards");o.innerHTML="",E.forEach((e,r)=>{const t=document.createElement("button");t.className=`taylor-card flex items-center gap-4 w-full bg-kernel-950 border-2 ${e.borderColor} rounded-lg p-4 cursor-pointer transition text-left group`,t.dataset.index=String(r),t.innerHTML=`
        <span class="text-sm font-bold ${e.color} shrink-0 w-48">${e.name}</span>
        <span class="font-mono text-xs text-kernel-400 flex-1">${e.formula}</span>
        <span class="text-[10px] text-kernel-600 group-hover:text-kernel-400 transition shrink-0">Click →</span>
      `,t.addEventListener("click",()=>n("taylor-detail","taylor-title","taylor-body",`<span class="${e.color}">${e.name}</span>`,e.detail,".taylor-card",r)),o.appendChild(t)})}function B(){const o=document.getElementById("lipschitz-cards"),e=[{output:"F",bound:"|ΔF| ≤ δ",meaning:"Most stable",color:"text-green-400",bg:"border-green-800"},{output:"S",bound:"|ΔS| ≤ 18.4·δ",meaning:"Moderate (ln(10⁸))",color:"text-amber-400",bg:"border-amber-800"},{output:"κ",bound:"|Δκ| ≤ 10⁸·δ",meaning:"Most sensitive (1/ε)",color:"text-red-400",bg:"border-red-800"}];o.innerHTML=e.map(r=>`
      <div class="bg-kernel-950 border-2 ${r.bg} rounded-lg p-4 text-center">
        <div class="text-2xl font-bold font-mono ${r.color} mb-1">${r.output}</div>
        <div class="font-mono text-xs text-kernel-400 mb-2">${r.bound}</div>
        <div class="text-[10px] text-kernel-500">${r.meaning}</div>
      </div>
    `).join("")}const M=[{section:"§1",name:"Duality",latin:"Complementum Perfectum",value:"0.0e+00",color:"text-amber-400",borderColor:"border-amber-700 hover:border-amber-500",detail:`<p><strong>What converges:</strong> F + ω across 10,000 random traces.</p>
        <p><strong>Max residual:</strong> 0.0 — literally zero, not "close to zero." The identity is exact by construction.</p>
        <p><strong>Why it matters:</strong> This is the zeroth-order check. If this ever fails, something is structurally broken. It cannot fail because ω is defined as 1 − F.</p>`},{section:"§2",name:"Integrity Bound",latin:"Limbus Integritatis",value:"Δ = 0.4447",color:"text-green-400",borderColor:"border-green-700 hover:border-green-500",detail:`<p><strong>What converges:</strong> For channels (0.95, 0.001) with equal weights, the gap Δ = F − IC = 0.4447.</p>
        <p><strong>Why this matters:</strong> IC ≤ F is <em>not trivially tight</em>. The gap can be enormous — nearly half the fidelity wasted because one channel is dead.</p>
        <p><strong>Proof sketch:</strong> IC = exp(κ) = exp(0.5·ln(0.95) + 0.5·ln(0.001)) = exp(−3.48) = 0.031. F = 0.5·(0.95 + 0.001) = 0.476. Δ = 0.445.</p>`},{section:"§3",name:"Geometric Slaughter",latin:"Trucidatio Geometrica",value:"IC/F: 1.0 → 0.11",color:"text-red-400",borderColor:"border-red-700 hover:border-red-500",detail:`<p><strong>What happens:</strong> Start with 8 channels all at 0.95 (IC/F = 1.0). Kill just 1 channel (set to ε). IC/F drops to 0.11.</p>
        <p><strong>Why "slaughter":</strong> F barely notices (0.95 → 0.83). But IC collapses (0.95 → 0.095). One dead channel in eight destroys 89% of integrity while fidelity only drops 13%.</p>
        <p><strong>Universal:</strong> This holds across all 23 domains. It is a structural property of the geometric mean, not domain-specific.</p>`},{section:"§4",name:"First Weld",latin:"Limen Generativum",value:"Γ(0.682) = 0.998",color:"text-cyan-400",borderColor:"border-cyan-700 hover:border-cyan-500",detail:`<p><strong>What converges:</strong> The drift cost Γ(ω) = ω³/(1−ω+ε) approaches 1.0 at ω_trap = 0.6823 — the Cardano root of x³ + x − 1 = 0.</p>
        <p><strong>The Cardano root:</strong> p = 3 is the unique integer such that ω³ + ω = 1 has an algebraic solution via Cardano's formula. This is not chosen — it is discovered.</p>
        <p><strong>Physical meaning:</strong> At ω_trap, the cost of drift equals the return budget. Systems that drift beyond this point face accelerating costs — the "wall" of the budget surface.</p>`},{section:"§5",name:"Confinement Cliff",latin:"Praecipitium Confinamenti",value:"IC/F: 0.94 → 0.01",color:"text-purple-400",borderColor:"border-purple-700 hover:border-purple-500",detail:`<p><strong>What happens:</strong> At the quark→hadron scale transition, IC/F drops from 0.94 (quarks) to 0.01 (hadrons). A 98% collapse.</p>
        <p><strong>Physical interpretation:</strong> Confinement destroys the color channel — it goes to ε. Since IC is a geometric mean, one dead channel kills the whole thing.</p>
        <p><strong>This is geometric slaughter in nature:</strong> The same mathematics that makes one dead channel kill IC in the lab makes confinement visible as a cliff in the kernel.</p>`},{section:"§6",name:"Scale Inversion",latin:"Inversio Scalarum",value:"IC/F: 0.01 → 0.96",color:"text-blue-400",borderColor:"border-blue-700 hover:border-blue-500",detail:`<p><strong>What happens:</strong> After the confinement cliff, at the atomic scale, IC/F recovers to 0.96 (e.g., Nickel).</p>
        <p><strong>How:</strong> New degrees of freedom (electronic, bulk properties) create new channels that are all healthy. The geometric mean recovers when all channels contribute.</p>
        <p><strong>Insight:</strong> Confinement is not destruction — it is a <em>phase transition</em> that kills old channels and births new ones. The scale ladder is: subatomic (IC/F ≈ 0.9) → confinement cliff (0.01) → atomic (0.96).</p>`},{section:"§8",name:"Equator Convergence",latin:"Aequator Collapsus",value:"S + κ = 0.0",color:"text-emerald-400",borderColor:"border-emerald-700 hover:border-emerald-500",detail:`<p><strong>What converges:</strong> At c = 1/2, entropy S and log-integrity κ perfectly cancel: S + κ = 0. Four quantities converge simultaneously.</p>
        <p><strong>The quintuple fixed point:</strong> At c = 1/2: (1) S = ln 2, (2) S + κ = 0, (3) h′(c) = 0, (4) g_F = 4, (5) θ = π/4. Five independent properties converge.</p>
        <p><strong>Why it matters:</strong> The equator is the point of maximal symmetry in the collapse field. It is the only point where entropy and log-integrity are in perfect balance.</p>`},{section:"§9",name:"Super-Exponential Convergence",latin:"Convergentia Super-Exponens",value:"28.5× shrinkage",color:"text-orange-400",borderColor:"border-orange-700 hover:border-orange-500",detail:`<p><strong>What converges:</strong> Under iterated collapse ωₙ = ω₀^{pⁿ} with p = 3, IC converges to 1 faster than exponentially.</p>
        <p><strong>The gap shrinks 28.5×</strong> per iteration — super-exponential because the exponent itself grows geometrically (3, 9, 27, 81...).</p>
        <p><strong>Physical meaning:</strong> Systems that survive one collapse round become dramatically more stable in the next. Return compounds — exactly as Axiom-0 predicts.</p>`},{section:"§10",name:"Seam Composition",latin:"Algebra Suturae",value:"|err| = 5.55e-17",color:"text-cyan-400",borderColor:"border-cyan-700 hover:border-cyan-500",detail:`<p><strong>What converges:</strong> The associativity residual of seam composition: (A ∘ B) ∘ C vs A ∘ (B ∘ C).</p>
        <p><strong>Residual:</strong> 5.55 × 10⁻¹⁷ — half a ULP (unit in the last place) of float64. This is as close to zero as binary arithmetic allows.</p>
        <p><strong>Algebraic structure:</strong> Seam chains form an abelian monoid — associative, commutative, with identity. This means multi-step validations are order-independent and O(1) composable.</p>`}];function A(){const o=document.getElementById("convergence-cards");o.innerHTML="",M.forEach((e,r)=>{const t=document.createElement("button");t.className=`conv-card flex items-center gap-4 w-full bg-kernel-950 border-2 ${e.borderColor} rounded-lg p-4 cursor-pointer transition text-left group`,t.dataset.index=String(r),t.innerHTML=`
        <span class="font-mono text-xs text-kernel-600 w-6 shrink-0">${e.section}</span>
        <span class="text-sm font-bold ${e.color} w-48 shrink-0">${e.name}</span>
        <span class="text-[10px] text-kernel-500 italic flex-1">${e.latin}</span>
        <span class="font-mono text-xs text-kernel-300 shrink-0 w-28 text-right">${e.value}</span>
        <span class="text-[10px] text-kernel-600 group-hover:text-kernel-400 transition shrink-0">→</span>
      `,t.addEventListener("click",()=>n("conv-detail","conv-title","conv-body",`<span class="${e.color}">${e.section} ${e.name}</span> <span class="text-kernel-500 text-sm">— ${e.latin}</span>`,`<div class="bg-kernel-900 rounded p-3 mb-3 text-center"><span class="font-mono text-lg ${e.color}">${e.value}</span></div>${e.detail}`,".conv-card",r)),o.appendChild(t)})}const q=[{gcd:"IC ≤ F",classical:"AM-GM Inequality (Euclid ≈ 300 BC)",stripped:"Channel semantics, weights, guard band, solvability, composition",color:"text-green-400",borderColor:"border-green-700 hover:border-green-500",detail:`<p>The integrity bound IC ≤ F generalizes the arithmetic-geometric mean inequality. But it has <em>five additional structures</em> that AM-GM lacks:</p>
        <ol class="list-decimal ml-4 space-y-1">
          <li><strong>Channel semantics:</strong> what channels <em>mean</em> (mass, charge, spin...).</li>
          <li><strong>Weighted means:</strong> non-uniform weights from the simplex.</li>
          <li><strong>Guard band (ε):</strong> principled handling of the pole at c = 0.</li>
          <li><strong>Solvability condition:</strong> IC ≤ F is required for c₁,₂ = F ± √(F²−IC²) to have real solutions.</li>
          <li><strong>Composition laws:</strong> IC geometric, F arithmetic, gap Hellinger-like.</li>
        </ol>
        <p class="mt-2">Strip all five → AM-GM. The classical result is the skeleton that remains when you remove the flesh.</p>`},{gcd:"F + ω = 1",classical:"Probability Conservation",stripped:"Cost function, phase diagram, regime structure",color:"text-amber-400",borderColor:"border-amber-700 hover:border-amber-500",detail:`<p>F + ω = 1 looks like probability conservation. But it carries:</p>
        <ul class="list-disc ml-4 space-y-1">
          <li><strong>A cost function:</strong> ω drives Γ(ω) = ω³/(1−ω+ε), which has a vertical asymptote at ω = 1.</li>
          <li><strong>A phase diagram:</strong> three regimes (Stable/Watch/Collapse) emerge from gates on ω and other invariants.</li>
          <li><strong>A semantic split:</strong> F and ω name concepts (fidelity/drift), not just numbers.</li>
        </ul>
        <p class="mt-2">Strip the cost, regime, and semantics → probability conservation. The constraint survives because it is the deepest structural fact.</p>`},{gcd:"IC = exp(κ)",classical:"Exponential Map (local)",stripped:"Domain universality, global scope",color:"text-purple-400",borderColor:"border-purple-700 hover:border-purple-500",detail:`<p>The classical exponential map is chart-specific (defined locally on a tangent space). GCD's IC = exp(κ) is:</p>
        <ul class="list-disc ml-4 space-y-1">
          <li><strong>Universal:</strong> works across all 23 domains with the same formula.</li>
          <li><strong>Global:</strong> valid for all c ∈ [0,1]ⁿ, not just near a base point.</li>
          <li><strong>Composable:</strong> seam chains compose additively in κ-space.</li>
        </ul>
        <p class="mt-2">Strip universality and globality → chart-specific exponential map.</p>`},{gcd:"S (Bernoulli field)",classical:"Shannon Entropy (1948)",stripped:"The collapse field (c ∈ (0,1))",color:"text-teal-400",borderColor:"border-teal-700 hover:border-teal-500",detail:`<p>Shannon entropy operates on discrete distributions: S = −Σ pᵢ ln pᵢ. This requires cᵢ ∈ {0, 1}.</p>
        <p>Bernoulli field entropy operates on continuous coherences: S = −Σ wᵢ[cᵢ ln cᵢ + (1−cᵢ)ln(1−cᵢ)]. Each channel contributes its own binary entropy.</p>
        <p><strong>What's gained:</strong> The full Bernoulli field has structure at every c ∈ (0,1). The equator (c = 1/2) is a special point where S + κ = 0. The Fisher metric g_F = 1/[c(1−c)] gives precision per unit noise. None of this exists in Shannon entropy.</p>
        <p class="mt-2 italic text-kernel-500">Shannon est limes degener — Shannon is the degenerate limit.</p>`},{gcd:"S ≤ h(F) (Entropy ceiling)",classical:"Fano's Inequality (1961)",stripped:"Observation-cost structure (epistemic weld)",color:"text-orange-400",borderColor:"border-orange-700 hover:border-orange-500",detail:`<p>Fano's inequality bounds error probability via entropy. The GCD version S ≤ h(F) bounds field entropy by binary entropy of fidelity. The GCD version has:</p>
        <ul class="list-disc ml-4 space-y-1">
          <li><strong>Tightness condition:</strong> equality iff homogeneous (all channels equal).</li>
          <li><strong>Composition behavior:</strong> tracks through subsystem merging.</li>
          <li><strong>Connection to observation cost:</strong> the epistemic weld (Theorem T9) shows that measurement itself has a κ cost.</li>
        </ul>`},{gcd:"Γ(ω) = ω³/(1−ω+ε)",classical:"Critical Slowing Down",stripped:"Exact exponent p=3, universality, cost ratio",color:"text-red-400",borderColor:"border-red-700 hover:border-red-500",detail:`<p>Classical critical slowing is a qualitative observation: systems near phase transitions slow down. GCD gives you the <em>exact</em> cost function with:</p>
        <ul class="list-disc ml-4 space-y-1">
          <li><strong>Exponent:</strong> p = 3 (unique integer yielding Cardano root x³+x−1=0).</li>
          <li><strong>Vertical asymptote:</strong> cost → ∞ as ω → 1. Not just slowing — divergence.</li>
          <li><strong>Cost ratio:</strong> Γ(0.99)/Γ(0.01) = 6,094,823 — six million to one separation.</li>
        </ul>`}];function D(){const o=document.getElementById("limits-cards");o.innerHTML="",q.forEach((e,r)=>{const t=document.createElement("button");t.className=`limits-card w-full bg-kernel-950 border-2 ${e.borderColor} rounded-lg p-4 cursor-pointer transition text-left group`,t.dataset.index=String(r),t.innerHTML=`
        <div class="flex items-center gap-3 mb-2">
          <span class="font-mono text-sm ${e.color} font-bold">${e.gcd}</span>
          <span class="text-kernel-600">→</span>
          <span class="text-sm text-kernel-400">${e.classical}</span>
        </div>
        <div class="flex items-center gap-2">
          <span class="text-[10px] text-red-400">Stripped:</span>
          <span class="text-[10px] text-kernel-500">${e.stripped}</span>
          <span class="text-[10px] text-kernel-600 group-hover:text-kernel-400 transition ml-auto">Click for depth →</span>
        </div>
      `,t.addEventListener("click",()=>n("limits-detail","limits-title","limits-body",`<span class="${e.color}">${e.gcd}</span> <span class="text-kernel-600">→</span> <span class="text-kernel-400">${e.classical}</span>`,e.detail,".limits-card",r)),o.appendChild(t)})}function H(){const o=document.getElementById("depth-comparison"),e=[{gcd:"IC ≤ F",layers:5,classical:"AM-GM",classicalLayers:1,extra:"semantics + weights + ε + solvability + composition"},{gcd:"F + ω = 1",layers:3,classical:"P(A) + P(Ā) = 1",classicalLayers:1,extra:"cost function + phase diagram + semantics"},{gcd:"S (Bernoulli)",layers:4,classical:"S (Shannon)",classicalLayers:1,extra:"continuous field + equator + Fisher metric + bounds"},{gcd:"Γ(ω) = ω³/(1−ω)",layers:3,classical:"Critical slowing",classicalLayers:0,extra:"exact exponent + cost ratio + Cardano root"}];o.innerHTML=e.map(r=>{const t=Array(r.layers).fill('<div class="w-4 h-4 bg-amber-500 rounded-sm"></div>').join(""),s=Array(r.classicalLayers).fill('<div class="w-4 h-4 bg-kernel-600 rounded-sm"></div>').join(""),i=Array(r.layers-r.classicalLayers).fill('<div class="w-4 h-4 bg-amber-500/30 border border-amber-600 rounded-sm"></div>').join("");return`
        <div class="bg-kernel-950 rounded-lg p-4">
          <div class="flex items-center gap-6 mb-2">
            <div class="w-32">
              <div class="font-mono text-sm text-amber-400">${r.gcd}</div>
              <div class="flex gap-0.5 mt-1">${t}</div>
            </div>
            <div class="text-kernel-600">vs</div>
            <div class="w-32">
              <div class="font-mono text-sm text-kernel-500">${r.classical}</div>
              <div class="flex gap-0.5 mt-1">${s}${i}</div>
            </div>
          </div>
          <div class="text-[10px] text-kernel-500">+${r.layers-r.classicalLayers} layers: ${r.extra}</div>
        </div>`}).join("")}const P=[{symbol:"Γ(ω)",name:"Drift Cost",formula:"ω³ / (1 − ω + ε)",role:"Debit",color:"text-red-400",borderColor:"border-red-700 hover:border-red-500",detail:`<p><strong>The cubic drift cost.</strong> Exponent p = 3 is the unique integer yielding the Cardano root x³ + x − 1 = 0 at ω_trap = 0.6823.</p>
        <p><strong>Three zones:</strong></p>
        <ul class="list-disc ml-4 space-y-1">
          <li>ω &lt; 0.038: Γ &lt; 5.5×10⁻⁵ — negligible cost (Stable)</li>
          <li>0.038 ≤ ω &lt; 0.30: Γ grows from 10⁻⁵ to 0.039 — manageable (Watch)</li>
          <li>ω ≥ 0.30: Γ surges toward infinity — prohibitive (Collapse)</li>
        </ul>
        <p><strong>Cost ratio:</strong> Γ(0.99)/Γ(0.01) = 6,094,823:1. The wall is not a metaphor — it is a divergent cost.</p>
        <p><strong>Cost elasticity:</strong> ε_Γ = ω(3−2ω)/(1−ω). At ω_trap, elasticity = 1 (unit-elastic — 1% more drift costs 1% more budget).</p>`},{symbol:"D_C",name:"Curvature Cost",formula:"α · C",role:"Debit",color:"text-orange-400",borderColor:"border-orange-700 hover:border-orange-500",detail:`<p><strong>Linear curvature cost</strong> with α = 1.0 (unit coupling). Higher heterogeneity → higher cost.</p>
        <p><strong>Crossover:</strong> Γ = D_C at ω ≈ 0.58. Below this, curvature dominates the cost; above, drift dominates.</p>
        <p><strong>Why linear?</strong> Curvature is already a standard deviation (root of variance) — it's "already a gradient." A linear cost is the natural first-order coupling.</p>`},{symbol:"R·τ_R",name:"Return Credit",formula:"R × τ_R",role:"Credit",color:"text-green-400",borderColor:"border-green-700 hover:border-green-500",detail:`<p><strong>The only credit term.</strong> R is the return rate, τ_R is the return delay. The product is the one-shot credit for demonstrated return.</p>
        <p><strong>If τ_R = ∞_rec:</strong> Credit = 0. No return → no credit. The system stays in deficit. This is not an error — it is a legitimate typed outcome.</p>
        <p><strong>Budget balance:</strong> PASS requires |Δκ_budget − Δκ_ledger| ≤ tol_seam = 0.005. The credit must cover the debits within tolerance.</p>`},{symbol:"Δκ",name:"Net Budget",formula:"R·τ_R − (D_ω + D_C)",role:"Balance",color:"text-cyan-400",borderColor:"border-cyan-700 hover:border-cyan-500",detail:`<p><strong>The conservation law.</strong> Net change in log-integrity = credit minus debits. The seam passes iff the residual |s| ≤ tol_seam.</p>
        <p><strong>Positive Δκ:</strong> System is gaining integrity. Return exceeds costs. The system is moving toward Stable.</p>
        <p><strong>Negative Δκ:</strong> System is losing integrity. Costs exceed return. The system is moving toward Collapse.</p>
        <p><strong>Zero Δκ:</strong> Perfect balance. The system is in steady state at its current regime.</p>`}];function W(){const o=document.getElementById("budget-cards");o.innerHTML="",P.forEach((e,r)=>{const t=document.createElement("button");t.className=`budget-card bg-kernel-950 border-2 ${e.borderColor} rounded-lg p-4 cursor-pointer transition text-left group`,t.dataset.index=String(r),t.innerHTML=`
        <div class="flex items-center gap-2 mb-2">
          <span class="font-mono text-lg ${e.color} font-bold">${e.symbol}</span>
          <span class="text-xs px-2 py-0.5 rounded ${e.role==="Debit"?"bg-red-900/30 text-red-400":e.role==="Credit"?"bg-green-900/30 text-green-400":"bg-cyan-900/30 text-cyan-400"}">${e.role}</span>
        </div>
        <div class="text-sm text-kernel-300 mb-1">${e.name}</div>
        <div class="font-mono text-xs text-kernel-500">${e.formula}</div>
        <div class="text-[10px] text-kernel-600 group-hover:text-kernel-400 transition mt-2">Click →</div>
      `,t.addEventListener("click",()=>n("budget-detail","budget-title","budget-body",`<span class="${e.color}">${e.symbol}</span> — ${e.name}`,e.detail,".budget-card",r)),o.appendChild(t)})}const _=[{name:"Flat Plain (Stable)",range:"ω < 0.038",slope:"< 0.005",color:"text-green-400",borderColor:"border-green-700 hover:border-green-500",detail:`<p>The budget surface is nearly flat — cost grows barely perceptibly. The system has abundant budget for return. Γ < 5.5×10⁻⁵.</p>
        <p><strong>Fisher Space:</strong> Only 12.5% of the manifold lives here. Stability is rare and precious.</p>`},{name:"Ramp (Watch)",range:"0.038 ≤ ω < 0.30",slope:"0.005 – 0.44",color:"text-amber-400",borderColor:"border-amber-700 hover:border-amber-500",detail:`<p>The cost grows steadily. The system can still return to Stable, but the budget margin shrinks. The slope increases from 0.005 to 0.44.</p>
        <p><strong>Fisher Space:</strong> 24.4% of the manifold. This is the "working zone" — most real systems live here during transitions.</p>`},{name:"Wall (Collapse)",range:"ω ≥ 0.30",slope:"0.44 → ∞",color:"text-red-400",borderColor:"border-red-700 hover:border-red-500",detail:`<p>The cost diverges toward infinity. The vertical asymptote at ω = 1 makes return prohibitively expensive.</p>
        <p><strong>Fisher Space:</strong> 63.1% of the manifold. Most of configuration space is in collapse. Return from here is what the axiom measures.</p>
        <p><strong>Gaussian curvature:</strong> K = 0 everywhere. The surface is flat/developable — a generalized cylinder (horn) over the Γ(ω) curve. All structure is from embedding.</p>`}];function R(){const o=document.getElementById("surface-zone-cards");o.innerHTML="",_.forEach((e,r)=>{const t=document.createElement("button");t.className=`surface-card flex items-center gap-4 w-full bg-kernel-950 border-2 ${e.borderColor} rounded-lg p-4 cursor-pointer transition text-left group`,t.dataset.index=String(r),t.innerHTML=`
        <span class="text-sm font-bold ${e.color} w-36 shrink-0">${e.name}</span>
        <span class="font-mono text-xs text-kernel-400 w-32 shrink-0">${e.range}</span>
        <span class="text-xs text-kernel-500 flex-1">slope: ${e.slope}</span>
        <span class="text-[10px] text-kernel-600 group-hover:text-kernel-400 transition shrink-0">Click →</span>
      `,t.addEventListener("click",()=>n("surface-detail","surface-title","surface-body",`<span class="${e.color}">${e.name}</span>`,e.detail,".surface-card",r)),o.appendChild(t)})}const z=[{rank:"1",dof:"1",color:"text-blue-400",borderColor:"border-blue-700 hover:border-blue-500",condition:"All cᵢ = c₀",detail:`<p><strong>Homogeneous systems.</strong> All channels are identical. This is the simplest and rarest case.</p>
        <p><strong>Properties:</strong> IC = F, C = 0, Δ = 0. The geometric mean equals the arithmetic mean because there is no heterogeneity.</p>
        <p><strong>Single DOF:</strong> Everything is determined by c₀ alone. All six kernel outputs are functions of one number.</p>
        <p><strong>Fast path:</strong> OPT-1 in the code (Lemmas 4, 10, 15) — detected early, 40% speedup.</p>`},{rank:"2",dof:"2",color:"text-orange-400",borderColor:"border-orange-700 hover:border-orange-500",condition:"Effective 2-channel",detail:`<p><strong>Effectively two-channel systems.</strong> C is determined by F and κ: C = g(F, κ).</p>
        <p><strong>Closed form:</strong> IC = √(F² − C²/4) for n = 2. The discriminant formula c₁,₂ = F ± √(F² − IC²) recovers both channels exactly.</p>
        <p><strong>Dimensionality:</strong> 2 DOF. This is the special case where curvature is not independent.</p>
        <p><strong>Physical examples:</strong> Binary systems, two-state systems, qubit-like objects.</p>`},{rank:"3",dof:"3",color:"text-green-400",borderColor:"border-green-700 hover:border-green-500",condition:"General (n ≥ 3)",detail:`<p><strong>The generic case.</strong> F, κ, C are mutually independent — knowing any two does not determine the third.</p>
        <p><strong>Maximum rank:</strong> Regardless of how many channels n you have (4, 8, 32, 64...), PCA at 99% variance consistently finds rank = 3. The kernel compresses n dimensions → 3.</p>
        <p><strong>Almost all real systems are rank-3.</strong> Rank-1 and Rank-2 are measure-zero subsets of the configuration space.</p>
        <p><strong>Inclusion:</strong> Rank-1 ⊂ Rank-2 ⊂ Rank-3. Lower ranks are specializations of higher rank.</p>`}];function N(){const o=document.getElementById("rank-cards");o.innerHTML="",z.forEach((e,r)=>{const t=document.createElement("button");t.className=`rank-card bg-kernel-950 border-2 ${e.borderColor} rounded-xl p-4 text-center cursor-pointer transition group`,t.dataset.index=String(r),t.innerHTML=`
        <div class="text-2xl font-bold font-mono ${e.color} mb-1">Rank ${e.rank}</div>
        <div class="text-sm text-kernel-300">${e.dof} DOF</div>
        <div class="text-[10px] text-kernel-500 mt-1">${e.condition}</div>
        <div class="text-[10px] text-kernel-600 group-hover:text-kernel-400 transition mt-2">Click →</div>
      `,t.addEventListener("click",()=>n("rank-detail","rank-title","rank-body",`<span class="${e.color}">Rank ${e.rank}</span> — ${e.dof} degree(s) of freedom`,e.detail,".rank-card",r)),o.appendChild(t)})}const G=[{name:"Fisher Information Metric",formula:"g_F(c) = 1 / [c(1−c)]",color:"text-amber-400",borderColor:"border-amber-700 hover:border-amber-500",detail:`<p><strong>Precision per unit noise.</strong> The Fisher metric is the reciprocal of the Bernoulli variance. It diverges at the poles (c → 0, 1) where a tiny change in c carries maximal information.</p>
        <p><strong>In Fisher coordinates (θ):</strong> c = sin²θ, and g_F(θ) = 4 (constant). The manifold is flat. All apparent structure comes from the embedding of the kernel's invariants on this flat line.</p>
        <p><strong>At the equator (c = 1/2):</strong> g_F = 4 — minimum information per parameter change. This is why the equator is the point of maximal uncertainty.</p>`},{name:"Bernoulli Field Entropy",formula:"Sᵢ = −[cᵢ ln cᵢ + (1−cᵢ) ln(1−cᵢ)]",color:"text-teal-400",borderColor:"border-teal-700 hover:border-teal-500",detail:`<p><strong>Per-channel uncertainty.</strong> The binary entropy function applied to each channel's coherence. Maximum at c = 1/2 (ln 2), zero at the poles.</p>
        <p><strong>Connection to Fisher metric:</strong> h″(c) = −g_F(c). The Hessian of entropy IS the (negated) Fisher metric. This is the Fano-Fisher duality (identity N5).</p>
        <p><strong>Connection to log-integrity:</strong> At c = 1/2, S + κ = 0 exactly. Entropy and log-integrity are complementary projections of one function: f(θ) = 2cos²θ · ln(tan θ).</p>`},{name:"Heterogeneity Gap",formula:"Δ = F − IC ≈ Var(c) / (2c̄)",color:"text-green-400",borderColor:"border-green-700 hover:border-green-500",detail:`<p><strong>The noise budget.</strong> The gap between fidelity (arithmetic mean) and integrity (geometric mean) measures channel heterogeneity. It equals the Fisher Information contribution from variance.</p>
        <p><strong>Taylor regime (C small):</strong> Δ ≈ C²/(8F). Quadratic in curvature.</p>
        <p><strong>Breakdown at c̄ ≈ 0.25:</strong> Below this, the Fisher approximation error exceeds 10%. The Poisson limit — noise proportional to signal — takes over.</p>
        <p><strong>Composition:</strong> Δ₁₂ = (Δ₁+Δ₂)/2 + (√IC₁−√IC₂)²/2. Unequal systems increase the gap.</p>`}];function O(){const o=document.getElementById("noise-cards");o.innerHTML="",G.forEach((e,r)=>{const t=document.createElement("button");t.className=`noise-card flex items-center gap-4 w-full bg-kernel-950 border-2 ${e.borderColor} rounded-lg p-4 cursor-pointer transition text-left group`,t.dataset.index=String(r),t.innerHTML=`
        <span class="text-sm font-bold ${e.color} w-48 shrink-0">${e.name}</span>
        <span class="font-mono text-xs text-kernel-400 flex-1">${e.formula}</span>
        <span class="text-[10px] text-kernel-600 group-hover:text-kernel-400 transition shrink-0">Click →</span>
      `,t.addEventListener("click",()=>n("noise-detail","noise-title","noise-body",`<span class="${e.color}">${e.name}</span>`,e.detail,".noise-card",r)),o.appendChild(t)})}const j=[{level:"L0",name:"The Axiom",color:"text-red-400",borderColor:"border-red-700 hover:border-red-500",items:['Axiom-0: "Collapse is generative; only what returns is real."'],detail:`<p>The single seed from which everything derives. It is a <em>constraint</em>, not a metaphor: every claim must be either derivable from this axiom or labeled as external.</p>
        <p><strong>What it constrains:</strong> Only phenomena that exhibit collapse-and-return are within scope. Phenomena that collapse without return are non-evaluable. Phenomena that don't collapse are uninteresting to the framework.</p>
        <p><strong>Operationally:</strong> "What returns is real" → τ_R must be finite. If τ_R = ∞_rec, the claim has zero credit.</p>`},{level:"L1",name:"Definitions",color:"text-amber-400",borderColor:"border-amber-700 hover:border-amber-500",items:["F = Σ wᵢcᵢ","κ = Σ wᵢ ln(cᵢ,ε)","S = −Σ wᵢ[cᵢ ln cᵢ + (1−cᵢ)ln(1−cᵢ)]","C = std(cᵢ)/0.5","ω = 1−F","IC = exp(κ)"],detail:`<p>The kernel function is defined by four primitive equations (F, κ, S, C) and two derived values (ω, IC). These definitions are not arbitrary — each captures a structural aspect of collapse:</p>
        <ul class="list-disc ml-4 space-y-1">
          <li>F measures <em>what survives</em> (the signal that persists)</li>
          <li>κ measures <em>logarithmic sensitivity</em> (multiplicative coherence in log space)</li>
          <li>S measures <em>uncertainty</em> of the collapse field</li>
          <li>C measures <em>heterogeneity</em> (coupling to uncontrolled DOF)</li>
        </ul>`},{level:"L2",name:"Algebraic Identities",color:"text-green-400",borderColor:"border-green-700 hover:border-green-500",items:["F + ω = 1 (duality)","IC ≤ F (integrity bound)","IC = exp(κ) (log-integrity)","S ≈ f(F,C) (statistical)"],detail:`<p>Three algebraic identities follow directly from the definitions. They are structural — they hold for <em>every</em> input, <em>every</em> domain.</p>
        <p><strong>F + ω = 1:</strong> ω is defined as 1−F. Exact by construction.</p>
        <p><strong>IC ≤ F:</strong> Geometric mean ≤ arithmetic mean. Plus: solvability, composition, semantics.</p>
        <p><strong>IC = exp(κ):</strong> Logarithm of geometric mean = sum of logs of factors.</p>
        <p>The statistical constraint S ≈ f(F,C) tightens with n via CLT.</p>
        <p><strong>Net effect:</strong> 6 outputs − 3 constraints = 3 effective DOF.</p>`},{level:"L3",name:"Lemmas (47)",color:"text-purple-400",borderColor:"border-purple-700 hover:border-purple-500",items:["L1–L9: Range, means, ε-control","L10–L18: Stability, Lipschitz","L19–L25: Seam composition","L26–L34: Coherence, calibration","L35–L47: Phase, slaughter, flux"],detail:`<p>47 lemmas prove properties of the kernel used by the implementation. Each is tagged OPT-* in the code.</p>
        <p><strong>Example chain:</strong> Lemma 4 (IC ≤ F) → Lemma 15 (homogeneity detection) → OPT-1 (fast path: 40% speedup when all channels equal).</p>
        <p><strong>The lemmas are not afterthoughts</strong> — they are the formal warranty that every optimization is correct. A lemma that fails would mean the code is wrong, not that the math needs changing.</p>`},{level:"L4",name:"Structural Identities (44)",color:"text-teal-400",borderColor:"border-teal-700 hover:border-teal-500",items:["E-series: 8 equator/exact","B-series: 12 bound/composition","D-series: 8 duality/deep","N-series: 16 network/integral"],detail:`<p>44 identities verified to machine precision, organized into four series and six connection clusters.</p>
        <p><strong>Some key results:</strong></p>
        <ul class="list-disc ml-4 space-y-1">
          <li>The manifold is <em>flat</em> in Fisher coordinates: g_F(θ) = 1</li>
          <li>S and κ are projections of <em>one function</em>: f(θ) = 2cos²θ · ln(tan θ), verified to &lt; 10⁻¹⁶</li>
          <li>∫g_F · S dc = π²/3 = 2ζ(2) — Riemann zeta connection</li>
          <li>5 closure diagnostics → 4 effective dimensions (low-rank algebra)</li>
        </ul>`},{level:"L5",name:"Domain Theorems (200+)",color:"text-orange-400",borderColor:"border-orange-700 hover:border-orange-500",items:["Standard Model: 27 theorems","Consciousness: 7 theorems","Awareness: 10 theorems","23 domains × domain-specific results"],detail:`<p>Tier-2 domain closures apply the kernel to specific fields — each produces domain theorems validated through the spine.</p>
        <p><strong>Standard Model:</strong> 27 theorems (10 core + 2 neutrino + 15 extended). Spin-statistics, generation monotonicity, confinement as IC collapse, mass-kernel log mapping.</p>
        <p><strong>Cross-domain universality:</strong> The same kernel, same frozen parameters, same identities hold across quarks, consciousness, finance, cosmology, semiotics. This is not analogy — it is structural identity.</p>`}];function V(){const o=document.getElementById("derivation-tree");o.innerHTML="",j.forEach((e,r)=>{const t=document.createElement("button"),s=r*8;t.className=`deriv-card w-full bg-kernel-950 border-2 ${e.borderColor} rounded-lg p-4 cursor-pointer transition text-left group`,t.style.marginLeft=`${s}px`,t.dataset.index=String(r),t.innerHTML=`
        <div class="flex items-center gap-3 mb-2">
          <span class="font-mono text-xs text-kernel-600">${e.level}</span>
          <span class="text-sm font-bold ${e.color}">${e.name}</span>
          ${r>0?'<span class="text-kernel-700 text-xs">← derived from above</span>':""}
          <span class="text-[10px] text-kernel-600 group-hover:text-kernel-400 transition ml-auto">Click →</span>
        </div>
        <div class="flex flex-wrap gap-1">${e.items.map(i=>`<span class="text-[10px] bg-kernel-900 text-kernel-400 px-2 py-0.5 rounded">${i}</span>`).join("")}</div>
      `,t.addEventListener("click",()=>n("deriv-detail","deriv-title","deriv-body",`<span class="${e.color}">${e.level}: ${e.name}</span>`,e.detail,".deriv-card",r)),o.appendChild(t)})}const K=[{name:"Equator Web",members:"E1, E4, E5, E6, B7, B9, D3",color:"text-purple-400",borderColor:"border-purple-700 hover:border-purple-500",detail:`<p>c = 1/2 is a <strong>quintuple fixed point</strong>: S = ln 2, S + κ = 0, h′ = 0, g_F = 4, θ = π/4. Five independent properties converge at one point.</p>
        <p>This is the mathematical center of the collapse field — the point of maximal symmetry where entropy and log-integrity are in perfect balance.</p>`},{name:"Dual Bounds",members:"E2, E3, B1, D1",color:"text-green-400",borderColor:"border-green-700 hover:border-green-500",detail:`<p>The kernel is <strong>sandwiched</strong>: IC ≤ F from below (integrity bound), S ≤ h(F) from above (entropy ceiling). Together: the kernel lives in a well-defined corridor.</p>
        <p>The solvability condition (D1) shows IC ≤ F is required for trace recovery: c₁,₂ = F ± √(F²−IC²).</p>`},{name:"Perturbation Chain",members:"B5, B11, D2, D7",color:"text-red-400",borderColor:"border-red-700 hover:border-red-500",detail:`<p>The derivation chain A6 → B3 → A2: the integrity bound follows from the kernel's <strong>own Taylor structure</strong>.</p>
        <p>κ ≈ ln F − C²/(8F²) + O(C⁴). Since −C²/(8F²) is always negative, exp(κ) ≤ F. The proof is internal — no external result needed.</p>`},{name:"Composition Algebra",members:"E8, B2, B3, B4, B12, D6, D8",color:"text-cyan-400",borderColor:"border-cyan-700 hover:border-cyan-500",detail:`<p><strong>IC geometric, F arithmetic, gap Hellinger-like.</strong> The composition laws are the algebraic structure that enables multi-system analysis.</p>
        <p>Seam chains form an abelian monoid. Gap is a composition invariant for identical systems. CV across channel kills ≈ 7×10⁻⁴ (IC democracy).</p>`},{name:"Fixed-Point Triangle",members:"B6, B8, B10, D4, D5",color:"text-blue-400",borderColor:"border-blue-700 hover:border-blue-500",detail:`<p>Three canonical points form the <strong>manifold skeleton</strong>:</p>
        <ul class="list-disc ml-4 space-y-1">
          <li>c = 1/2 — equator (maximal symmetry)</li>
          <li>c* = 0.7822 — logistic self-dual fixed point (maximal S + κ per channel)</li>
          <li>c_trap = 0.3178 — trap point (Cardano root reflection)</li>
        </ul>
        <p>Connected by the reflection formula: c_trap = 1 − c*. The three points partition the manifold into geometrically distinct regions.</p>`},{name:"Spectral Family",members:"E7, N1, N11, N13",color:"text-pink-400",borderColor:"border-pink-700 hover:border-pink-500",detail:`<p>f = S + κ is <strong>spectrally complete</strong>: all polynomial moments ∫₀¹ cⁿ f dc have closed forms involving harmonic numbers.</p>
        <p><strong>Key integral:</strong> ∫₀¹ g_F · S dc = π²/3 = 2ζ(2). The Riemann zeta function appears naturally from the Fisher-entropy product.</p>
        <p>The coupling centroid ∫₀¹(S + κ)·c dc = 0 — the mean position of f is exactly 1/2. The function is balanced around the equator.</p>`}];function U(){const o=document.getElementById("cluster-cards-math");o.innerHTML="",K.forEach((e,r)=>{const t=document.createElement("button");t.className=`cluster-math-card bg-kernel-950 border-2 ${e.borderColor} rounded-lg p-4 cursor-pointer transition text-left group`,t.dataset.index=String(r),t.innerHTML=`
        <div class="flex items-center gap-2 mb-1">
          <span class="text-sm font-bold ${e.color}">${e.name}</span>
          <span class="text-[10px] text-kernel-600 group-hover:text-kernel-400 transition ml-auto">Click →</span>
        </div>
        <div class="text-[10px] font-mono text-kernel-500">${e.members}</div>
      `,t.addEventListener("click",()=>n("cluster-detail-math","cluster-math-title","cluster-math-body",`<span class="${e.color}">${e.name}</span> <span class="text-kernel-500 text-sm">[${e.members}]</span>`,e.detail,".cluster-math-card",r)),o.appendChild(t)})}const Q=[{symbol:"ε",value:"10⁻⁸",name:"Guard Band",color:"text-amber-400",borderColor:"border-amber-700 hover:border-amber-500",detail:`<p><strong>Why this value:</strong> The pole at ω = 1 (c = 0) does not affect any measurement to machine precision with ε = 10⁻⁸.</p>
        <p>For float64, the smallest representable positive number above 0 is ~5×10⁻³²⁴. An ε of 10⁻⁸ is astronomically larger — it's a conservative guard that prevents numerical issues while being invisible to all physical measurements.</p>
        <p><strong>How discovered:</strong> Vary ε from 10⁻¹ to 10⁻¹⁵. At 10⁻⁸, kernel outputs stabilize to within machine epsilon for all 23 domains. Smaller values add no information; larger values introduce bias.</p>`},{symbol:"p",value:"3",name:"Drift Exponent",color:"text-red-400",borderColor:"border-red-700 hover:border-red-500",detail:`<p><strong>Why this value:</strong> p = 3 is the <em>unique integer</em> such that ω^p + ω = 1 has an algebraic solution via Cardano's formula. The root is ω_trap = 0.6823.</p>
        <p><strong>p = 1:</strong> linear cost — too weak to separate regimes.</p>
        <p><strong>p = 2:</strong> ω² + ω = 1 gives the golden ratio conjugate — but only 2 regime zones separate cleanly.</p>
        <p><strong>p = 3:</strong> ω³ + ω = 1 gives Cardano root — 3 regime zones separate cleanly (Stable/Watch/Collapse). This is the minimal polynomial that produces three distinct zones with a well-defined trap.</p>
        <p><strong>p ≥ 4:</strong> no new zones, same qualitative behavior, but worse numerical conditioning.</p>`},{symbol:"α",value:"1.0",name:"Curvature Coupling",color:"text-orange-400",borderColor:"border-orange-700 hover:border-orange-500",detail:`<p><strong>Why this value:</strong> Unit coupling — curvature cost is at the same scale as drift cost. The crossover Γ = D_C occurs at ω ≈ 0.58.</p>
        <p>If α were ≠ 1: the crossover would shift, changing the balance between drift and heterogeneity in the budget. α = 1 is the unique value where the two costs have equal standing in the budget equation.</p>`},{symbol:"tol",value:"0.005",name:"Seam Tolerance",color:"text-cyan-400",borderColor:"border-cyan-700 hover:border-cyan-500",detail:`<p><strong>Why this value:</strong> The width where IC ≤ F holds at 100% across all 23 domains. This is discovered empirically by sweeping tolerance values and checking pass rates.</p>
        <p><strong>Tighter (0.001):</strong> Some domains fail at boundary cases — too strict.</p>
        <p><strong>Looser (0.01):</strong> Some nonconformant cases pass — too lax.</p>
        <p><strong>0.005:</strong> The sweet spot where all 23 domains pass and all genuine violations are caught.</p>`},{symbol:"c*",value:"0.7822",name:"Logistic Self-Dual Fixed Point",color:"text-green-400",borderColor:"border-green-700 hover:border-green-500",detail:`<p><strong>Definition:</strong> The unique c ∈ (1/2, 1) satisfying c* = σ(1/c*), where σ is the logistic function. Equivalently: ln((1−c)/c) + 1/c = 0.</p>
        <p><strong>What it maximizes:</strong> S + κ per channel. The point where entropy production and log-integrity are in optimal balance.</p>
        <p><strong>Mirror:</strong> c_trap = 1 − c* ≈ 0.3178 via the reflection formula. The Fixed-Point Triangle: {1/2, c*, c_trap} spans the full structure.</p>`}];function Z(){const o=document.getElementById("frozen-cards");o.innerHTML="",Q.forEach((e,r)=>{const t=document.createElement("button");t.className=`frozen-card flex items-center gap-4 w-full bg-kernel-950 border-2 ${e.borderColor} rounded-lg p-4 cursor-pointer transition text-left group`,t.dataset.index=String(r),t.innerHTML=`
        <span class="font-mono text-lg ${e.color} font-bold w-8 shrink-0">${e.symbol}</span>
        <span class="font-mono text-sm text-kernel-300 w-16 shrink-0">${e.value}</span>
        <span class="text-sm text-kernel-400 flex-1">${e.name}</span>
        <span class="text-[10px] text-kernel-600 group-hover:text-kernel-400 transition shrink-0">Click →</span>
      `,t.addEventListener("click",()=>n("frozen-detail","frozen-title","frozen-body",`<span class="${e.color}">${e.symbol} = ${e.value}</span> — ${e.name}`,e.detail,".frozen-card",r)),o.appendChild(t)})}const h={stable:[.95,.92,.98,.93,.96,.91,.94,.97],watch:[.8,.75,.85,.7,.82,.78,.76,.81],collapse:[.4,.35,.5,.3,.45,.25,.55,.2],slaughter:[.95,.95,.95,.001,.95,.95,.95,.95],equator:[.5,.5,.5,.5,.5,.5,.5,.5],cstar:[l,l,l,l,l,l,l,l]};function J(){const o=document.getElementById("lab-channels");o.innerHTML="";for(let e=0;e<8;e++)o.innerHTML+=`
        <div class="text-center">
          <label class="text-[10px] text-kernel-500 block mb-1">c<sub>${e+1}</sub></label>
          <input type="number" id="lab-c${e}" value="${h.stable[e]}" step="0.01" min="0" max="1"
            class="lab-input bg-kernel-800 border border-kernel-700 rounded px-2 py-1 w-full text-sm text-kernel-200 text-center" />
        </div>
      `;document.querySelectorAll(".lab-preset").forEach(e=>{e.addEventListener("click",()=>{const r=e.dataset.preset;h[r].forEach((s,i)=>{document.getElementById(`lab-c${i}`).value=String(s)}),g()})}),document.querySelectorAll(".lab-input").forEach(e=>{e.addEventListener("input",g)}),g()}function g(){const o=[];for(let a=0;a<8;a++){const u=document.getElementById(`lab-c${a}`);o.push(Math.max(0,Math.min(1,parseFloat(u.value)||0)))}const e=b(o),r=f(e),t=y(e),s=v(e.omega,e.C,1,1),i=document.getElementById("lab-outputs"),m=[{sym:"F",val:e.F,color:"text-amber-400"},{sym:"ω",val:e.omega,color:"text-red-400"},{sym:"S",val:e.S,color:"text-teal-400"},{sym:"C",val:e.C,color:"text-orange-400"},{sym:"κ",val:e.kappa,color:"text-purple-400"},{sym:"IC",val:e.IC,color:"text-green-400"},{sym:"Δ",val:e.delta,color:"text-pink-400"}];i.innerHTML=m.map(a=>`
      <div class="flex items-center justify-between bg-kernel-950 rounded px-3 py-2">
        <span class="font-mono text-sm ${a.color} font-bold w-6">${a.sym}</span>
        <span class="font-mono text-sm text-kernel-200">${a.val.toFixed(6)}</span>
      </div>
    `).join("");const c=document.getElementById("lab-identities");c.innerHTML=t.map(a=>`
      <div class="flex items-center justify-between bg-kernel-950 rounded px-3 py-2">
        <span class="text-xs text-kernel-300">${a.formula}</span>
        <span class="font-mono text-xs ${a.pass?"text-green-400":"text-red-400"}">${a.pass?"PASS":"FAIL"} (${a.residual.toExponential(1)})</span>
      </div>
    `).join("");const d=document.getElementById("lab-regime"),p=x[r.regime]||{bg:"bg-kernel-800",text:"text-kernel-200"};d.innerHTML=`
      <div class="flex items-center gap-3 mb-2">
        <span class="px-2 py-1 rounded text-xs font-bold ${p.bg} ${p.text}">${r.regime}</span>
        ${r.isCritical?'<span class="px-2 py-1 rounded text-xs font-bold bg-purple-900 text-purple-200">CRITICAL</span>':""}
      </div>
      <div class="grid grid-cols-2 gap-2 text-xs">
        <div class="bg-kernel-950 rounded px-2 py-1"><span class="text-kernel-500">Γ(ω):</span> <span class="text-kernel-300 font-mono">${s.gamma.toFixed(6)}</span></div>
        <div class="bg-kernel-950 rounded px-2 py-1"><span class="text-kernel-500">D_C:</span> <span class="text-kernel-300 font-mono">${s.D_C.toFixed(6)}</span></div>
        <div class="bg-kernel-950 rounded px-2 py-1"><span class="text-kernel-500">Δκ:</span> <span class="text-kernel-300 font-mono">${s.deltaKappa.toFixed(6)}</span></div>
        <div class="bg-kernel-950 rounded px-2 py-1"><span class="text-kernel-500">Seam:</span> <span class="font-mono ${s.pass?"text-green-400":"text-red-400"}">${s.pass?"PASS":"FAIL"}</span></div>
      </div>
    `}function n(o,e,r,t,s,i,m){const c=document.getElementById(o);document.getElementById(e).innerHTML=t,document.getElementById(r).innerHTML=s,c.classList.remove("hidden"),document.querySelectorAll(i).forEach((d,p)=>{p===m?d.classList.add("ring-2","ring-kernel-500"):d.classList.remove("ring-2","ring-kernel-500")}),c.scrollIntoView({behavior:"smooth",block:"nearest"})}const X=["inv-detail-close","alg-id-close","comp-close","sens-close","taylor-close","conv-close","limits-close","budget-close","surface-close","rank-close","noise-close","deriv-close","cluster-math-close","frozen-close"];X.forEach(o=>{const e=document.getElementById(o);e&&e.addEventListener("click",()=>{const r=e.closest("[id]");r&&r.classList.add("hidden")})});k();F();I();S();L();B();A();D();H();W();R();N();O();V();U();Z();J();
