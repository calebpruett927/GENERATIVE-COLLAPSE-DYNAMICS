{
  "name": "gcd-kernel-sites",
  "type": "module",
  "version": "2.3.1",
  "description": "Autonomous domain sites powered by the GCD Headless Contract Gateway",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "generate": "cd .. && python -m umcp.hcg.builder --all --output web/src/content && cd web",
    "generate:domain": "cd .. && python -m umcp.hcg.builder --output web/src/content && cd web",
    "full-build": "npm run generate && npm run build"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.0.0",
    "@types/three": "^0.183.1",
    "astro": "^5.0.0",
    "three": "^0.183.2"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^6.0.0",
    "tailwindcss": "^3.4.0",
    "vitest": "^4.1.0"
  }
}
