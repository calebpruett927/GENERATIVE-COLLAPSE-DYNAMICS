# UMCP Docker Compose Configuration
# 
# Usage:
#   docker-compose -f deploy/docker-compose.yml up -d
#   docker-compose -f deploy/docker-compose.yml down
#   docker-compose -f deploy/docker-compose.yml logs -f

version: '3.8'

services:
  dashboard:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.dashboard
    container_name: umcp-dashboard
    ports:
      - "8501:8501"
      - "8000:8000"
    environment:
      - UMCP_DEV_MODE=true
      - DASHBOARD_PORT=8501
      - API_PORT=8000
    volumes:
      - ../ledger:/app/ledger
      - ../logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Optional: Nginx reverse proxy for production
  # nginx:
  #   image: nginx:alpine
  #   container_name: umcp-nginx
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - dashboard
  #   restart: unless-stopped

networks:
  default:
    name: umcp-network
